<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.techclutch.finassist">

    <uses-feature android:name="android.hardware.camera" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LandingActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".banktypes.BankTypeActivity" />
        <activity
            android:name=".questionaires.HomeQuestionaireActivity"
            android:label="@string/title_activity_home_questionaire"
            android:theme="@style/AppTheme" />
        <activity android:name=".forms.HomeFormActivity"></activity>
        <activity android:name=".uploaddocuments.UploadDocumentActivity"></activity>
    </application>

</manifest>